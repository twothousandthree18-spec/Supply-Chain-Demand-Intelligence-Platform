# Supply Chain & Demand Intelligence Platform
# Phase 1 - Data Acquisition & Data Quality
# Reproducible pinned dependencies. Install into .venv:
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt

# Data acquisition (official M5 via Kaggle)
kaggle==1.6.17

# Data profiling / diagnostics
pandas>=2.2,<4
numpy>=2.0,<3
matplotlib>=3.8

# Testing (data-quality test suite)
pytest>=8

# PostgreSQL (Phase 2 Data Engineering / Analytical Warehouse)
psycopg2-binary>=2.9

# Phase 3 - Analytics & Forecasting & Inventory
# Forecasting / statistics (ETS/ARIMA, statistical tests) and ML (gradient
# boosting - deferred to a later Phase 3 pilot; scikit-learn provides the
# split-free validation/metric utilities used by forecasting).
statsmodels>=0.14
scikit-learn>=1.4
scipy>=1.12

# Statistical plotting for analytics diagnostics
seaborn>=0.13

# Phase 6 - Web / Product Presentation Layer
# API + static frontend. FastAPI (ASGI) serves aggregated/derived data from
# PostgreSQL with typed (pydantic) contracts; uvicorn runs the dev server.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# Testing the Phase 6 API layer
httpx>=0.27,<1
